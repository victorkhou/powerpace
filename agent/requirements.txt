# Core agent stack
langchain>=0.3.27
langchain-anthropic>=0.3.0
langgraph>=0.2.50
langsmith>=0.1.140

# Sidecar service
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Tests (no DB / no secrets required — all external calls are mocked)
pytest>=8.0.0

# Data access (read-only queries against Supabase/Postgres)
supabase>=2.9.0

# Phase 4: drop in additional providers here, e.g.
# langchain-openai>=0.2.0
